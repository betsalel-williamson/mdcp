---
'@bwilliamson/skill-mdcp-arch-research': patch
---

Add the research-project archetype. It defines a `research/` tier for dated records that are superseded rather than edited in place, kept separate from product shards so the current-docs rule stays enforceable in both tiers. Ships as a work-in-progress archetype: `metadata.internal: true`, out of `skills.sh.json`, installable locally with `INSTALL_INTERNAL_SKILLS=1`.
