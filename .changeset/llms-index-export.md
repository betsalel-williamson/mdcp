---
'@bwilliamson/mdcp-cli': minor
'@bwilliamson/mdcp-core': minor
---

Add `mdcp export --llms-index` to generate a versioned agent bootstrap file (`mdcp.v1.llms.txt`, protocol 1.0.0.0) in the docs root. Config gains `protocolVersion` and `export.llmsIndex.outputFile`.
