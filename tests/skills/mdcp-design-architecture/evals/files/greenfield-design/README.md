# Fake greenfield repo for design-architecture evals

Isolated fixture: MDCP guide stubs exist, but **no** architecture shards yet.
Treat this directory as the working-tree root when evaluating
`mdcp-design-architecture`. Do not edit the real monorepo `docs/` or `packages/`.
