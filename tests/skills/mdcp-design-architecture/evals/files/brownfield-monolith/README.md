# Fake brownfield repo for design-architecture evals

Isolated fixture with a **single large architecture monolith** under `docs/`
plus thin MDCP feature/ADR indexes. Treat this directory as the working-tree
root when evaluating `mdcp-design-architecture`. Do not edit the real
monorepo `docs/` or `packages/`.
