# Agent work-item tracking

Stub WORK_ITEM_LOOKUP for the design-architecture eval fixture.

```text
Integration branch=main (pull before branching)
Feature branches=descriptive (e.g. feature/issue-N-short-slug)
One branch per WORK_ITEM=do not mix unrelated designs
Branch before work=create the feature branch before shards or code
Docs=describe current behavior only; planning and backlogs stay in the tracker
ADRs=docs/features/adr/
```

Load this shard for delivery conventions. Do not invent tracker scope beyond the provided WORK_ITEM.
