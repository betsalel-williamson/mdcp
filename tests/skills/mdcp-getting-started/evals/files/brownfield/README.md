# mdcp-eval-brownfield

Minimal Node project with **legacy** documentation under `docs/` and **no** MDCP config or four-tier guide layout. Used as the starting workspace for brownfield `mdcp-getting-started` live evals.

Existing files must stay in place during bootstrap; content is pulled into new shards and legacy paths are marked ready to archive after review.
