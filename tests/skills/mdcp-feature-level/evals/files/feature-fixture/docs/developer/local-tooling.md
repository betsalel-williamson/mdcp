# Local tooling

Maintainer-only workflows for working on this repo. These are not shipped product
capabilities and have no end-user surface.

- `pnpm build` — compile all packages.
- `pnpm test` — run the unit suite.
