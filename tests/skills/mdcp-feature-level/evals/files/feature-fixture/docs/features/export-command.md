# Export command

`example-cli export` writes the current report to stdout.

## Capabilities

- Serializes the in-memory report to JSON on stdout.
- Exit code `0` on success; non-zero on write failure.
