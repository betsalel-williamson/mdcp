# Reusable skills

`example-cli` ships a library of reusable **skills** that end users invoke to
automate common workflows.

## Capabilities

- Ships a catalog of named skills users can run by name.
- Each skill declares its inputs and expected output.
- Users list available skills with `example-cli skills list`.
