# Plan

**WORK_ITEM:** Add one sentence to `docs/features/compile.md` noting dry-run is planned.

**Helper:** `mdcp-doc-only` (shard edit only; no product code).

## Steps (after approval)

1. Edit `docs/features/compile.md` — add one sentence that dry-run compile is planned (current-docs wording; no ticket or backlog text in the shard).
2. Run `mdcp compile`, then `mdcp check`.
3. Stop for human review before commit.

## Notes

- Minimal plan per leadership; feature branching and commit grouping treated as optional polish and deferred.
- Single shard, single concern — no numbered commit groups required.
