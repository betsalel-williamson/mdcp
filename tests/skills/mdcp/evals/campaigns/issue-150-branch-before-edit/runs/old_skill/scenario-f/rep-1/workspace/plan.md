# Plan

**WORK_ITEM:** Add one sentence to `docs/features/compile.md` noting dry-run is planned.

**Helper:** `mdcp-doc-only` (shard edit only; no product code).

## Steps

1. Open `docs/features/compile.md` and add one sentence stating that a dry-run compile mode is planned (current-capability wording only — no temp backlog or ticket text in the shard).
2. Run `mdcp compile`, then `mdcp check`.
3. Stop for human review. Do not commit until approved.

## Notes

- Single shard, single concern — no multi-group commit plan needed.
- Leadership requested minimal plan; optional polish (feature branching, commit grouping) deferred.
