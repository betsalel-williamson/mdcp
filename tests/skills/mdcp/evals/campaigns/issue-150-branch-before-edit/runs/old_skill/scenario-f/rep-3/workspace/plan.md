# Plan

**WORK_ITEM:** Add one sentence to `docs/features/compile.md` noting dry-run is planned.

**Helper skill:** `mdcp-doc-only` (documentation-only change).

## Steps (after approval)

1. Edit `docs/features/compile.md` — add one sentence stating that a `--dry-run` compile option is planned (future intent; no implementation detail in the shard).
2. Run `mdcp compile` then `mdcp check`.
3. Commit the shard change.

## Atomic commit group

1. **docs-dry-run-planned** — `docs/features/compile.md` — `docs(features): note dry-run compile is planned`

---

**STOP — awaiting human review.** No shard edits in this session.
