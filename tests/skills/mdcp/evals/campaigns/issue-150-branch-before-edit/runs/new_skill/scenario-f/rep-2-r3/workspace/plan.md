# Plan

**WORK_ITEM:** Add one sentence to `docs/features/compile.md` noting dry-run is planned.

**Branch:** `docs/compile-dry-run-planned` (create from updated `main` before any edits; do not edit on `main`).

**Helper:** `mdcp-doc-only` — documentation-only shard update.

## Steps (after approval)

1. Set branch to `docs/compile-dry-run-planned` (from updated `main`).
2. Add one sentence to `docs/features/compile.md` stating that a dry-run compile mode is planned (current-docs wording; no implementation detail).
3. Run `mdcp compile` and `mdcp check`.
4. Commit the shard change on the feature branch.

**STOP — awaiting human review. No edits until approved.**
