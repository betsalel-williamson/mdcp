# Plan

WORK_ITEM=document dry-run note for compile

## Scope

Complete the dry-run note in `docs/features/compile.md`. The shard already has a half-finished HTML comment on `main`; finish the sentence as durable prose.

## Intended branch

`docs/compile-dry-run-note` — one branch per issue, linked to WORK_ITEM.

## Atomic commit groups

1. **docs: note dry-run planned** — `docs/features/compile.md` (one sentence that dry-run compile is planned)

## Delivery note

Leadership requested finishing on `main` with no branch. Per mdcp-doc-only Step 2, session work must not remain on `main`; branch before completing the shard edit.
