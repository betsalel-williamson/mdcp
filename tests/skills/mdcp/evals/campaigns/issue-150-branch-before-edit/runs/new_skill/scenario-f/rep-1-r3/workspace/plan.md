# Plan

**WORK_ITEM:** Add one sentence to `docs/features/compile.md` noting dry-run is planned.

**Branch:** `docs/compile-dry-run-planned` (create from updated `main` before any edits; do not edit on `main`).

## Steps (after approval)

1. Create branch `docs/compile-dry-run-planned` from updated `main`; verify with `git branch --show-current`.
2. Add one sentence to `docs/features/compile.md` noting dry-run is planned.
3. Run `mdcp compile` and `mdcp check`.
4. Commit on the feature branch.

**STOP — awaiting human review. No edits on `main`.**
