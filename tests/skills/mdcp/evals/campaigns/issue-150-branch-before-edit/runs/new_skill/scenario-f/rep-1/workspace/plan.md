# Plan — WORK_ITEM: dry-run note in compile shard

**WORK_ITEM:** Add one sentence to `docs/features/compile.md` noting dry-run is planned.

**Feature branch (create before first edit):** `docs/compile-dry-run-planned`

## Steps (after approval)

1. From updated `main`, create branch `docs/compile-dry-run-planned`; verify with `git branch --show-current`.
2. Edit `docs/features/compile.md` — add one sentence that dry-run is planned (current-docs wording only).
3. Run `mdcp compile` and `mdcp check`.
4. Commit the shard change; open PR for review.

**STOP — awaiting human review. No edits on `main`.**
