# Fixture Mini Repo

Eval sandbox for `mdcp-doc-only`.

- Product truth: Widget Export supports **JSON and CSV**.
- Durable docs must not hold backlogs or temporary planning notes.
- Config lives at `docs/mdcp.config.json`. From `docs/`, run `mdcp compile` and `mdcp check --skip-vale` (Vale is optional in this sandbox).
