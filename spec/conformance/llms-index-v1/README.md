# llms-index-v1 conformance vector

Protocol version: **1.0.0.0**

## Required

1. First line: `mdcp-llms-index: 1.0.0.0`
2. Filename matches `mdcp.v{version}.llms.txt` where `{version}` abbreviates by dropping trailing `.0` segments
3. Document includes sections: Adoption path, Query instructions, Glossary, Validation

## Fixture

See [sample.md](sample.md) for a minimal valid document body (after header).
