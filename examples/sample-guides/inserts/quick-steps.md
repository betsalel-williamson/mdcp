1. Add captioned insert shards under typed libraries (`diagrams/`, `tables/`, `figures/`, `media/`).
2. Link shards from guide catalogs with normal markdown.
3. Enable `inlineInserts` in `guides[].compile.hooks`.

- Unordered lists render as GFM bullet lists.
- **Bold** and _italic_ inline formatting pass through unchanged.
